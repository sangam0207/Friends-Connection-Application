import { SentienceSDK } from '../../../sdk';
/**
 * Initializes and sets up the PlatformManager for the SentienceSDK instance.
 *
 * This function constructs a new PlatformManager with various settings, such as
 * deep linking preferences, wake lock status, and debug mode, based on the provided
 * options within the SentienceSDK instance. The initialized PlatformManager is then
 * stored back into the SentienceSDK instance for further use.
 *
 * @param instance The current instance of the SentienceSDK, which contains user-defined or default options.
 * @returns {Promise<void>} A Promise that resolves when the PlatformManager has been successfully set up.
 */
export declare function setupPlatformManager(instance: SentienceSDK): Promise<void>;
//# sourceMappingURL=setupPlatformManager.d.ts.map